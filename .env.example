# Copiar a .env (ignorado por git) y rellenar.
# SQL Server exige: 8+ caracteres con mayúsculas, minúsculas, números y símbolos.
SA_PASSWORD=Cambiar.Esta1
# 32+ caracteres. Genera una con: openssl rand -base64 48
JWT_KEY=
FRONTEND_ORIGIN=http://localhost:4200
